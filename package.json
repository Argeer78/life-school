{
  "name": "lifeschool",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "server.js",
  "engines": {
    "node": "22.x"
  },
  "workspaces": [
    "platform/steward-app"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=platform/steward-app",
    "build": "npm run build --workspace=platform/steward-app",
    "start": "node server.js",
    "typecheck": "npm run typecheck --workspace=platform/steward-app",
    "test": "npm run test --workspace=platform/steward-app"
  }
}
